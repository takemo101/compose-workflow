{
	"scripts": {
		"test": "vitest run .opencode",
		"test:watch": "vitest .opencode",
		"format": "biome format .opencode/plugin --write",
		"lint": "biome check .opencode/plugin --write --unsafe"
	},
	"dependencies": {
		"@opencode-ai/plugin": "1.1.12"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.11",
		"vitest": "^4.0.16"
	}
}
