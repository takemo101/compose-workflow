{
	"scripts": {
		"setup": "bash scripts/setup.sh",
		"test": "vitest run .opencode",
		"test:watch": "vitest .opencode",
		"format": "biome format .opencode .claude --write",
		"lint": "biome check .opencode .claude --write --unsafe"
	},
	"dependencies": {
		"@opencode-ai/plugin": "1.1.12"
	},
	"pi": {
		"skills": [
			".pi/skills"
		]
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.11",
		"vitest": "^4.0.16"
	}
}
